# SpaceX Project Frontend

Это Frontend приложение для проекта SpaceX, разработанное на **Nuxt 4** и **Tailwind CSS**.

> **Info**: Проект использует современную файловую структуру Nuxt 4 с директорией `app/`.

## 🛠 Технологический стек
- **Фреймворк:** [Nuxt](https://nuxt.com/) (v4)
- **Стилизация:** [Tailwind CSS](https://tailwindcss.com/)
- **Пакетный менеджер:** npm

## 📂 Структура проекта

```
FE/
├── app/                # Основной исходный код (Nuxt 4)
│   ├── pages/          # Страницы приложения (File-based routing)
│   ├── components/     # Vue компоненты
│   ├── layouts/        # Макеты страниц
│   └── app.vue         # Корневой компонент
├── public/             # Статические файлы (доступны в корне /)
├── nuxt.config.ts      # Конфигурация Nuxt
├── tailwind.config.js  # Конфигурация Tailwind CSS
└── package.json        # Зависимости и скрипты
```

## 🚀 Начало работы

### 1. Установка
Перейдите в папку и установите зависимости:
```bash
cd FE
npm install
```

### 2. Запуск в режиме разработки
```bash
npm run dev
```
Сервер будет доступен по адресу:
👉 [http://localhost:3000](http://localhost:3000) (или `:3001`, если порт занят)

### 3. Сборка (Build)
Для подготовки к деплою:
```bash
npm run build
```
Результат сборки будет в директории `.output/`.

Для предпросмотра сборки:
```bash
npm run preview
```

## 📐 Code Style
- Проект использует **ESLint** с конфигурацией для Nuxt.
- Запуск линтера: `npx nuxt lint` (или через ESLint плагин в IDE).
