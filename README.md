# SpaceX Project Frontend

Это Frontend приложение для проекта SpaceX, разработанное на **Nuxt 4** и **Tailwind CSS**.

## 🛠 Технологический стек
- **Фреймворк:** [Nuxt](https://nuxt.com/) (v4)
- **Стилизация:** [Tailwind CSS](https://tailwindcss.com/)
- **Управление состоянием:** Nuxt Composables / Pinia (если используется)

## 🚀 Начало работы

### 1. Требования
- [Node.js](https://nodejs.org/) (рекомендуется v20+)

### 2. Установка
Перейдите в директорию frontend и установите зависимости:
```bash
cd FE
npm install
```

### 3. Запуск сервера разработки
Запустите dev-сервер:
```bash
npm run dev
```
Приложение будет доступно по адресу `http://localhost:3000` (или `http://localhost:3001`, если порт 3000 занят бэкендом). Точный URL смотрите в выводе терминала.

### 4. Сборка для продакшн
Чтобы собрать приложение для продакшн среды:
```bash
npm run build
```
Это создаст оптимизированную сборку в директории `.output`.

Для предпросмотра продакшн сборки локально:
```bash
npm run preview
```

## 📂 Структура проекта
- `app/` - Основной исходный код приложения
- `public/` - Статические ресурсы
- `nuxt.config.ts` - Конфигурация Nuxt
- `tailwind.config.js` - Конфигурация Tailwind CSS
